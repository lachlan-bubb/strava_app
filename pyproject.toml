[tool.poetry]
name = "strava-app"
version = "0.1.0"
description = ""
authors = ["Lachlan Bubb <lachlan_bubb@live.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
